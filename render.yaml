version: "1"

services:
  # ------------------------------------
  # 1. Backend Node.js Server
  # ------------------------------------
  - type: web
    name: requestflow-server
    env: docker # Tells Render to use your server/Dockerfile
    plan: free
    rootDir: server
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production

      - key: DATABASE_URL
        value: "PASTE_NEON_URL_HERE"
      - key: JWT_SECRET
        generateValue: true
      - key: CLIENT_URL
        value: "*" # To prevent CORS errors initially
      - key: EMAIL_USER
        value: "PASTE_GMAIL_HERE"
      - key: EMAIL_PASS
        value: "PASTE_APP_PASSWORD_HERE"
      - key: ADMIN_EMAIL
        value: "admin@tps.com"
      - key: ADMIN_PASSWORD
        value: "PASTE_ADMIN_PASSWORD_HERE"

  # ------------------------------------
  # 2. Frontend React Client
  # ------------------------------------
  - type: web
    name: requestflow-client
    env: static
    rootDir: client
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      # Required for React Router to work properly on static hosts
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_URL
        value: "PASTE_SERVER_URL_HERE"
      - key: VITE_SOCKET_URL
        value: "PASTE_SERVER_URL_HERE"

